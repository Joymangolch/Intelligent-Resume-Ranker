#```gitignore
# --- Python General ---
# Byte-compiled / optimized / C extensions
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtual Environment ---
# Exclude the virtual environment folder to keep the repo small
venv/
.venv/
env/
.env/
ENV/
.env

# --- IDE / Editor Files ---
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/

# --- OS-generated files ---
# macOS
.DS_Store
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Datasets and Sensitive Files ---
# Ignore the data folder to prevent committing large or private CSVs
data/
*.csv```gitignore
# --- Python General ---
# Byte-compiled / optimized / C extensions
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtual Environment ---
# Exclude the virtual environment folder to keep the repo small
venv/
.venv/
env/
.env/
ENV/
.env

# --- IDE / Editor Files ---
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/

# --- OS-generated files ---
# macOS
.DS_Store
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Datasets and Sensitive Files ---
# Ignore the data folder to prevent committing large or private CSVs
data/
*.csv

# --- Model Artifacts - VERY IMPORTANT FOR DEPLOYMENT ---
# Right now, your Streamlit Cloud app NEEDS this folder in the repository to work.
# If you ignore this folder, your deployed app will break again.
#
# ONLY uncomment the line below if you plan to store your artifacts elsewhere
# (like in cloud storage, e.g., an S3 bucket) and have your app download them.
# For now, KEEP THIS COMMENTED OUT.
# artifacts/

```

# --- Model Artifacts - VERY IMPORTANT FOR DEPLOYMENT ---
# Right now, your Streamlit Cloud app NEEDS this folder in the repository to work.
# If you ignore this folder, your deployed app will break again.
#
# ONLY uncomment the line below if you plan to store your artifacts elsewhere
# (like in cloud storage, e.g., an S3 bucket) and have your app download them.
# For now, KEEP THIS COMMENTED OUT.
# artifacts/

```